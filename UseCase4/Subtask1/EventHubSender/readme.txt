1. Install python3 (link for Windows, apt install python3 python3-pip for ubuntu, or use any package manager that you want)
2. Open powershell or linux terminal, change dir to downloaded project, and then type and run pip install -r requirements.txt
3. Change parametes in the script as you need (setup credentials, setup dir with files and batch size)
4. run python3 main.py